version: '3.3'
networks:
  net-mhpasswordmanager-mode-localhost:
    driver: bridge
services:
  config-services:
    image: dkloc.starlord443.dev/marcoshssilva/password-manager/mhpasswordmanager-config-services:latest
    env_file:
      - envs/config-services.env
    networks:
      - net-mhpasswordmanager-mode-localhost
    ports:
      - "8888:8888"
